import React from "react";
import Loader from 'react-loader-spinner';

const Spinner = () => (
    <Loader
        type="Bars"
        color="#fff"
        height={30}
        width={30}
    />
);

export default Spinner;
